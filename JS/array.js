// Reduce()

// Description. The reduce() method executes a reducer function for array element. The reduce() method returns a single value: the function's accumulated result.

// arr1 = [1,2,3,4,5,6,7]

// const store = arr1.reduce((acc,item) => {
//       return  acc += item
// },0)

// console.log("store",store)

const name = "aarya"

const student = {
    name : "Kavya"
}
console.log("student.name before ===>",student.name)
student.name = "Bavya"

console.log("student.name after ===>",student.name)
name = "Selvi";
console.log(name)


